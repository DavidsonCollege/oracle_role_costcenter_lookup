{
  "name": "oracle-access-request-lookup",
  "version": "1.3.2",
  "description": "Oracle Access Request Lookup desktop app",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "prebuild:mac": "node -e \"require('fs').writeFileSync('build-date.json', JSON.stringify({date: new Date().toISOString().slice(0,10)}))\"",
    "prebuild:win": "node -e \"require('fs').writeFileSync('build-date.json', JSON.stringify({date: new Date().toISOString().slice(0,10)}))\"",
    "build:mac": "electron-builder --mac",
    "build:win": "electron-builder --win",
    "build": "electron-builder --mac --win"
  },
  "build": {
    "appId": "edu.davidson.oracle-access-request-lookup",
    "productName": "Oracle Access Request Lookup",
    "icon": "assets/icon",
    "publish": {
      "provider": "github",
      "owner": "DavidsonCollege",
      "repo": "oracle_role_costcenter_lookup",
      "releaseType": "release"
    },
    "files": [
      "main.js",
      "preload.js",
      "renderer.js",
      "index.html",
      "build-date.json",
      "assets/**",
      "node_modules/xlsx/**",
      "node_modules/electron-updater/**"
    ],
    "mac": {
      "category": "public.app-category.business",
      "icon": "assets/icon.icns",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "entitlements.mac.plist",
      "entitlementsInherit": "entitlements.mac.plist",
      "notarize": true,
      "target": [
        {
          "target": "dmg",
          "arch": [
            "universal"
          ]
        }
      ]
    },
    "dmg": {
      "title": "Oracle Access Request Lookup"
    },
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "installerHeaderIcon": null
    }
  },
  "devDependencies": {
    "electron": "^40.6.0",
    "electron-builder": "^26.8.1"
  },
  "dependencies": {
    "electron-updater": "^6.8.3",
    "xlsx": "^0.18.5"
  }
}
